# Patches — integration-v0.11.3

**Base upstream tag:** `v0.11.3` (SHA `2a960a59fe1dbbd35282f0556b3666d81102e781`)
**Regenerated:** 2026-09-09

## Patch series

| #    | File | Description |
| ---- | ---- | ----------- |
| 0001 | `0001-chore-reconcile-fork-customizations-onto-v0.11.3-base.patch` | Full cumulative diff of all fork customizations reapplied onto `v0.11.3`: Prompt Insights, analytics/routing/cross-filter, NIS2 access-log/audit/OIDC compliance, MCP import compatibility, Alembic multi-head safety, dependency/security pin refreshes, and shipped UI fixes |
| 0002 | `0002-chore-bump-version-to-0.11.3-260909.patch` | Version bump to `0.11.3-260909` |

> **Note:** this series was generated by diffing `f9590b8017199e56d5e953657e6498e3cef1d246..main`, applying that cumulative fork diff onto `v0.11.3`, and then resolving the overlapping upstream files by hand on `integration-v0.11.3`.
> Material integration resolutions and follow-up hardening:
> - `backend/open_webui/utils/middleware.py`, `backend/open_webui/main.py`, and `backend/open_webui/utils/tools.py`: retained the upstream request/stream pipeline, router registrations, compatibility endpoints, and built-in tool handling while preserving routing-usage metadata, MCP wiring, audit/access-log ordering, OIDC back-channel logout, subagent flags, and mutating-memory guardrails.
> - `backend/open_webui/utils/oauth.py`, `backend/open_webui/routers/users.py`, `backend/open_webui/routers/retrieval.py`, and `backend/open_webui/routers/openai.py`: combined upstream OAuth/session, user-role, SSRF, and pooled-streaming changes with fork cache invalidation, OIDC claim capture, session disconnects, Linkup support, and the existing stream chunk guard.
> - `backend/open_webui/retrieval/utils.py` and `backend/open_webui/retrieval/web/utils.py`: kept the upstream SSRF adapter/session behavior and the fork blocked-host checks, including every import required by the merged call sites.
> - `backend/open_webui/routers/chats.py`, `backend/open_webui/utils/automations.py`, `backend/open_webui/utils/chat_variables.py`, `backend/open_webui/utils/models.py`, and `backend/open_webui/utils/payload.py`: retained upstream chat search and automation trigger changes while preserving internal-chat authorization, channel automation delivery, and required `JSONCodec` behavior.
> - `src/lib/components/admin/Analytics/Dashboard.svelte` and the fork-only analytics paths: restored the shipped Prompt Insights, routing, and cross-filter UI while retaining the integrated v0.11.3 backend contracts.
> - Prompt Insights follow-up fixes: made trend updates portable, run claims and result writes atomic, scheduler execution tracked and non-blocking, and the active-claim migration safe for MySQL/MariaDB and partially applied schemas; the Alembic branches were merged to one head.
> - `backend/open_webui/middleware/access_log.py`: added explicit v0.11.3 provider-model, retrieval URL, memories reindex, and admin-only Ollama compatibility rules, and corrected obsolete Ollama embedding route patterns.

## Applying

```bash
git checkout v0.11.3
git apply patches/0001-chore-reconcile-fork-customizations-onto-v0.11.3-base.patch
git apply patches/0002-chore-bump-version-to-0.11.3-260909.patch
```
